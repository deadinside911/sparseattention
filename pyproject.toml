[project]
name = "sparseattention"
version = "0.1.0"
description = "Implementing sparse attention from scratch"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.5.3",
    "torch>=2.14.0",
]
